[package]
name = "entity"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
name = "entity"
path = "src/lib.rs"

[dependencies]
chrono = { version = "^0.4", features = ["serde"] }
sea-orm = { version = "^0" }
serde = { version = "^1", features = ["derive"] }